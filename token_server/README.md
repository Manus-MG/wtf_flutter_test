# Token Server

Local HTTP server for minting 100ms tokens.

## Endpoint

- `GET /token?userId=&role=`

## Setup

1. Copy `.env.example` to `.env`.
2. Fill in the 100ms credentials.
3. Run the server from this folder.
